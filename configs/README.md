Configs
=======

This directory holds configuration files for running and benchmarking.

Notes
-----
- Use `default.yaml` as a starting point for local runs.
- Avoid committing secrets. Prefer environment variables or secret stores.


