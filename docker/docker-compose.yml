version: "3.9"
services:
  pqc-benchmark:
    image: pqc-benchmarking-skeleton:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    command: ["echo", "Placeholder compose; no functional code."]


