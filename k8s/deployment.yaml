apiVersion: apps/v1
kind: Deployment
metadata:
  name: pqc-benchmark
  namespace: pqc-benchmark
  labels:
    app: pqc-benchmark
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pqc-benchmark
  template:
    metadata:
      labels:
        app: pqc-benchmark
    spec:
      containers:
        - name: pqc-benchmark
          image: docker.io/library/pqc-benchmarking-skeleton:latest
          command: ["echo", "Placeholder deployment; no functional code."]
          imagePullPolicy: IfNotPresent


