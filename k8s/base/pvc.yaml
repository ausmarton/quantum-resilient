apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: quantum-resilient-results
  labels:
    app: quantum-resilient
    component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

